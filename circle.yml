machine:
  node:
    version: '7'
dependencies:
  pre:
    - echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" >> ~/.npmrc
deployment:
  npm:
    tag: /^v/
    owner: taskworld
    commands:
      - npm publish

