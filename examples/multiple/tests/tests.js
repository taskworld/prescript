const { test, action } = require('../../..')

test('Test 1', () => {
  action('Test 1 action', () => { })
})

test('Test 2', () => {
  action('Test 2 action', () => { })
})

test('Test 3', () => {
  action('Test 3 action', () => { })
})
